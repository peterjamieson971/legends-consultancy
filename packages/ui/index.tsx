// Shared UI components will be exported from here
// This package is for components that might be shared across multiple apps

export const Button = () => {
  return <button>Shared Button Component</button>
}
